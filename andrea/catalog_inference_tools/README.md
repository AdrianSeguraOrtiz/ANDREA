# ANDREA Inference Catalog

Packaged runtime catalog used by `andrea infer-network`.

This directory is part of the Python package and is intended to be shipped in the wheel/sdist.
It contains:
- JSON schemas used by the orchestrator for validation/compatibility checks
- Declarative input validation specs consumed by preflight (CLI/GUI/core)
- Tool metadata (`toolspec.json`) for each inference tool, including provenance and short method descriptors
- Optional benchmark-derived planning profiles (`cost.json`) when available

Wrapper tooling (Dockerfiles, runtime wrappers, image build scripts, benchmarks, smoketests, fixtures)
lives in `wrappers/inference_tools/`.

## Layout

```text
andrea/catalog_inference_tools/
  schemas/
    toolspec.schema.json
    toolcost.schema.json
    dataset-manifest.schema.json
    input-spec.schema.json
  input_specs/
    expression_matrix.json
    groups.json
    cell_phenotypes.json
    lineage_tree.json
    tf_list.json
    prior_grn.json
    prior_grn_by_group.json
  tools/
    <tool_id>/
      toolspec.json
      cost.json            # optional, generated by benchmark_costs.py
```

## Runtime Contract (container IO)

Each tool container is executed with:

```bash
/app/run_tool.sh \
  --input /io/expression.tsv \
  --params /io/params.json \
  --extra /io/extra \
  --output-dir /io/out \
  --threads 8
```

When a plan is executed by the ANDREA orchestrator, `/io/execution.json` is also
mounted next to `params.json` and contains the selected `execution.mode`
(`global`, `group_native`, or `group_emulated`). Existing wrappers may ignore it.

Expected outputs:
- `/io/out/network.csv`
- `/io/out/progress.json`

`network.csv` required columns:
- `source`
- `target`
- `score`
- `sign`
- `evidence`
- `context`

## Schemas

- `schemas/toolspec.schema.json`: ToolSpec contract.
- `schemas/toolcost.schema.json`: Cost profile contract for `tools/<tool_id>/cost.json`.
- `schemas/dataset-manifest.schema.json`: orchestrator input manifest (includes embedded dataset spec).
- `schemas/input-spec.schema.json`: contract for declarative per-input validation rules.
- `input_specs/*.json`: concrete validation rules used in preflight for expression/extras.

## Notes

- The orchestrator reads this catalog from inside the installed `andrea` package.
- Tool sources used to build Docker images (e.g. `Dockerfile`, `run_tool.py`)
  intentionally live outside the packaged runtime catalog in `wrappers/inference_tools/tools/`.
- Smoke/benchmark parameter overrides derived from ToolSpec defaults live in
  `wrappers/inference_tools/param_overrides/`.
- Per-tool smoketest configs and their schema are intentionally *not* stored here; they live in:
  - `wrappers/inference_tools/tests/smoketest_configs/`
  - `wrappers/inference_tools/tests/schemas/smoketest.config.schema.json`
